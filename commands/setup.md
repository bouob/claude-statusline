---
description: Set up claude-statusline by registering it in ~/.claude/settings.json
allowed-tools: Bash
---

Run the setup script to register claude-statusline:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/setup.js"
```

After the script completes, tell the user to restart Claude Code for the statusline to take effect.
